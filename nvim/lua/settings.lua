vim.g.mapleader = " " --Define " " as leader

require("vimrc") --Basic vim look (keep this for use in vim)
require("pack") --Install all packages needed using lazy
require("lsp") --Install all packages needed using lazy
require("lf") --Install all packages needed using lazy
