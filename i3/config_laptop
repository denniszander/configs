# Make sure only external monitor runs if connected
exec toggle_monitor.sh -e

# Get some applications running
exec spotify-launcher
# Background picture 
exec feh --bg-scale /media/data/Wallpaper/odeceixe.jpg

